import React,{useEffect,useMemo,useState} from "react";
import Optimize from "./Optimize";

const FEATURES=["Nitrogen","Phosphorus","Potassium","Temperature","Humidity","pH_Value","Rainfall"];
const labels={Nitrogen:"Nitrógeno",Phosphorus:"Fósforo",Potassium:"Potasio",Temperature:"Temperatura",Humidity:"Humedad",pH_Value:"pH del suelo",Rainfall:"Precipitación"};
const units={Nitrogen:"mg/kg",Phosphorus:"mg/kg",Potassium:"mg/kg",Temperature:"°C",Humidity:"%",pH_Value:"pH",Rainfall:"mm"};
const defaults={Nitrogen:70,Phosphorus:50,Potassium:60,Temperature:24,Humidity:70,pH_Value:6.5,Rainfall:130};
const presets={Balanced:{...defaults},"Wet tropical":{Nitrogen:85,Phosphorus:45,Potassium:40,Temperature:28.5,Humidity:88,pH_Value:6.2,Rainfall:260},"Dryland":{Nitrogen:40,Phosphorus:35,Potassium:30,Temperature:32,Humidity:40,pH_Value:7.4,Rainfall:45},"Cool highland":{Nitrogen:50,Phosphorus:90,Potassium:120,Temperature:15,Humidity:75,pH_Value:5.8,Rainfall:140}};
const ranges={Nitrogen:[0,180],Phosphorus:[5,150],Potassium:[5,210],Temperature:[5,50],Humidity:[10,100],pH_Value:[3.5,9.5],Rainfall:[20,350]};
const datasetLabels={Wheat:"Trigo",Potato:"Papa",Maize:"Maíz",Tomato:"Tomate",Sugarcane:"Caña de azúcar",Rice:"Arroz",Silt:"Limoso",Clay:"Arcilloso",Saline:"Salino",Sandy:"Arenoso",Peaty:"Turboso",Loamy:"Franco"};
const displayDatasetLabel=(value)=>datasetLabels[value]||value;
async function api(url,options){const r=await fetch(url,options);if(!r.ok)throw new Error(`La solicitud no pudo completarse (${r.status}).`);return r.json()}
function Icon({children}){return <span className="nav-icon" aria-hidden="true">{children}</span>}
function Layout({route,children}){const links=[["/","Resumen","◌"],["/analysis","Análisis","⌁"],["/regions","Regiones","⌖"],["/recommend","Recomendación","＋"],["/optimize","Plan rentable","↗"]];const [healthy,setHealthy]=useState(null);useEffect(()=>{api("/api/health").then(()=>setHealthy(true)).catch(()=>setHealthy(false))},[]);return <div className="shell"><aside className="sidebar"><a className="brand" href="/"><span className="mark"/><div><strong>TERRA</strong><small>inteligencia agronómica</small></div></a><div className="nav-label">Explora Terra</div><nav className="nav" aria-label="Navegación principal">{links.map(([href,text,icon])=><a key={href} href={href} className={route===href?"active":""}><Icon>{icon}</Icon><span>{text}</span></a>)}</nav><div className="side-foot"><div className="health"><i className={`dot ${healthy===false?"offline":""}`}/>{healthy===null?"Conectando…":healthy?"Terra disponible":"No se pudo conectar"}</div>Inteligencia de campo, hecha legible.</div></aside><main className="main">{children}</main></div>}
function Header({eyebrow,title,description,action}){return <><h1 className="page-title">{title}</h1>{description&&<p className="intro">{description}</p>}{action}{eyebrow==="04 / Recomendación"&&<a className="button secondary" href="/optimize" style={{display:"inline-block",marginTop:18}}>Explorar el plan de cultivo →</a>}</>}
function Section({title,sub,children,action}){return <section><div className="section-head"><div><h2>{title}</h2>{sub&&<p>{sub}</p>}</div>{action}</div>{children}</section>}
function State({loading,error,empty,children}){if(loading)return <div className="panel loading">Leyendo registros de campo…</div>;if(error)return <div className="error"><b>No se pudo leer el registro de campo.</b><br/>{error}<br/><button className="button secondary" onClick={()=>location.reload()} style={{marginTop:12}}>Intentar de nuevo</button></div>;if(empty)return <div className="panel result-empty">Aún no hay observaciones disponibles.</div>;return children}
function Metric({label,value,note}){return <div className="metric"><div className="metric-label">{label}</div><div className="metric-value">{value}</div>{note&&<div className="metric-note">{note}</div>}</div>}
function Bars({items=[],accent=""}){const max=Math.max(...items.map(x=>x.value||0),1);return <div>{items.slice(0,8).map((x,i)=><div className="bar-row" key={x.name}><div className="bar-meta"><b>{displayDatasetLabel(x.name)}</b><span>{x.share!=null?`${Number(x.share).toFixed(1)}%`:x.value}</span></div><div className="bar-track"><div className={`bar-fill ${accent}`} style={{width:`${Math.max(4,(x.value||0)/max*100)}%`}}/></div></div>)}</div>}
function Overview(){const [data,setData]=useState(null),[error,setError]=useState("");useEffect(()=>{api("/api/overview").then(setData).catch(e=>setError(e.message))},[]);const m=data?.metrics||{};return <Layout route="/"><Header eyebrow="01 / Resumen de campo" title={<>Lee el terreno<br/><em>tal como es.</em></>} description="Project Terra convierte siete señales de suelo y clima en ecorregiones funcionales: patrones que puedes inspeccionar, cuestionar y convertir en decisiones."/><State loading={!data&&!error} error={error}><div className="metrics"><Metric label="Muestras de campo" value={m.samples?.toLocaleString()||"—"} note="observaciones indexadas"/><Metric label="Clases de cultivo" value={m.crops||"—"} note="etiquetas históricas"/><Metric label="Ecorregiones" value={m.regions||"—"} note="grupos funcionales"/><Metric label="Señales" value={m.features||"—"} note={`${m.soils||"—"} tipos de suelo`}/></div><Section title="Señal del conjunto" sub="Una lectura general de la evidencia"><div className="grid-2"><div className="panel"><h3>Composición de cultivos</h3><p className="panel-caption">Etiquetas de cultivo registradas en la muestra de campo.</p><Bars items={data?.cropDistribution} /></div><div className="panel"><h3>Composición del suelo</h3><p className="panel-caption">Tipos de suelo en el mismo conjunto de observaciones.</p><Bars items={data?.soilDistribution} accent="alt"/></div></div></Section><Section title="Qué mide Terra" sub="Rangos de variables, en sus unidades nativas"><div className="panel"><div className="feature-list">{(data?.featureStats||[]).map((f,i)=><div className="feature-line" key={f.feature}><b>{labels[f.feature]||f.feature}</b><div className="spark"><i style={{width:`${35+(i*9)%58}%`}}/></div><em>{f.mean!=null?Number(f.mean).toFixed(1):"—"} {units[f.feature]||""}</em></div>)}</div><div className="note">El modelo agrupa observaciones por similitud ambiental, no por fronteras políticas. Usa el análisis para ver dónde se sostienen esas similitudes y dónde no.</div></div></Section><section className="callout" style={{marginTop:54}}><h3>¿Quieres leer una parcela?</h3><p>Introduce un perfil de suelo y clima. Terra lo ubicará en la ecorregión funcional más cercana y mostrará la evidencia del resultado.</p><a className="button" href="/recommend">Ver la recomendación →</a></section></State></Layout>}
function Scatter({points=[]}){const colors=["#b96b4b","#719b68","#6b9fa0","#d79b51","#7f78a5"];return <div className="scatter"><svg viewBox="0 0 700 380" role="img" aria-label="Gráfico de dispersión de componentes principales"><path d="M52 330H665M52 330V24" stroke="#cfd8ce" fill="none"/><path d="M52 252H665M52 174H665M52 96H665" stroke="#dfe5dc"/>{points.slice(0,500).map((p,i)=><circle key={i} cx={55+((Number(p.x)||0)+4)/8*600} cy={322-((Number(p.y)||0)+4)/8*285} r="3.2" fill={colors[Number(p.cluster)%5]||colors[i%5]} opacity=".6"/>)}</svg></div>}
function Analysis(){const [data,setData]=useState(null),[overview,setOverview]=useState(null),[error,setError]=useState(""),[tab,setTab]=useState("pca");useEffect(()=>{Promise.all([api("/api/pca"),api("/api/overview")]).then(([a,b])=>{setData(a);setOverview(b)}).catch(e=>setError(e.message))},[]);const tabs=[["pca","Proyección PCA"],["features","Variables ambientales"],["clusters","Comparación de grupos"]];return <Layout route="/analysis"><Header eyebrow="02 / Análisis" title={<>Mide las<br/><em>diferencias.</em></>} description="Explora las distribuciones y proyecciones que sostienen las cinco ecorregiones funcionales de Terra."/><State loading={!data&&!error} error={error}><div className="tabs" role="tablist" aria-label="Vistas de análisis">{tabs.map(([key,label])=><button key={key} role="tab" aria-selected={tab===key} tabIndex={tab===key?0:-1} className={`tab ${tab===key?"active":""}`} onClick={()=>setTab(key)} onKeyDown={e=>{if(e.key==="ArrowRight"||e.key==="ArrowDown"){e.preventDefault();setTab(tabs[(tabs.findIndex(x=>x[0]===key)+1)%tabs.length][0])}if(e.key==="ArrowLeft"||e.key==="ArrowUp"){e.preventDefault();setTab(tabs[(tabs.findIndex(x=>x[0]===key)+tabs.length-1)%tabs.length][0])}}}>{label}</button>)}</div>{tab==="pca"&&<div className="grid-2"><div className="panel" style={{gridColumn:"span 2"}}><h3>Proyección de componentes principales</h3><p className="panel-caption">Una vista bidimensional de la similitud ambiental en siete dimensiones. Cada punto es una observación.</p><Scatter points={data?.points}/><div className="legend">{[["#b96b4b","Ecorregión 0"],["#719b68","Ecorregión 1"],["#6b9fa0","Ecorregión 2"],["#d79b51","Ecorregión 3"],["#7f78a5","Ecorregión 4"]].map(x=><span key={x[1]}><i style={{background:x[0]}}/>{x[1]}</span>)}</div><div className="note">PC1 explica {(data?.explainedVariance?.[0]||0).toFixed(1)}% de la varianza; PC2 explica {(data?.explainedVariance?.[1]||0).toFixed(1)}%. La proyección sirve para inspección; el agrupamiento opera en las siete dimensiones.</div></div></div>}{tab==="features"&&<div className="grid-2"><div className="panel"><h3>Distribuciones ambientales</h3><p className="panel-caption">Media y extensión observada en unidades originales.</p><div className="feature-list">{(overview?.featureStats||[]).map((f,i)=><div className="feature-line" key={f.feature}><b>{labels[f.feature]||f.feature}</b><div className="spark"><i style={{width:`${40+(i*8)%50}%`,background:i%2?"#6b9fa0":"#b96b4b"}}/></div><em>{Number(f.min||0).toFixed(1)}—{Number(f.max||0).toFixed(1)}</em></div>)}</div></div><div className="panel"><h3>Distribución de cultivos</h3><p className="panel-caption">Las etiquetas de referencia junto a las señales del modelo.</p><Bars items={overview?.cropDistribution} accent="sun"/></div></div>}{tab==="clusters"&&<div className="panel"><h3>Distribución de ecorregiones</h3><p className="panel-caption">Proporción de la muestra observada en cada grupo funcional.</p><Bars items={overview?.clusterDistribution} accent="alt"/></div>}</State></Layout>}
function Regions(){const [data,setData]=useState(null),[error,setError]=useState(""),[selected,setSelected]=useState(0);useEffect(()=>{api("/api/regions").then(setData).catch(e=>setError(e.message))},[]);const region=data?.regions?.[selected];return <Layout route="/regions"><Header eyebrow="03 / Regiones" title={<>Cinco formas de<br/><em>comportarse.</em></>} description="Las ecorregiones funcionales son perfiles, no etiquetas: combinaciones recurrentes de nutrientes, clima y agua."/><State loading={!data&&!error} error={error}><div className="region-grid">{(data?.regions||[]).map((r,i)=><button className={`region-card ${selected===i?"selected":""}`} key={r.id} onClick={()=>setSelected(i)} aria-pressed={selected===i}><span className="region-no">0{i+1} / ECOR</span><h3>Región {r.id}</h3><strong>{Number(r.share||0).toFixed(1)}%</strong><small>{r.count?.toLocaleString()} observaciones</small><div className="profile-bars">{Object.entries(r.normalizedProfile||{}).slice(0,4).map(([k,v])=><span className="profile-mini" key={k}>{(labels[k]||k).slice(0,3)}<i style={{width:`${Math.max(10,Number(v)*50)}px`}}/></span>)}</div></button>)}</div>{region&&<div className="detail grid-2"><div className="panel"><span className="kicker">Región {region.id} / perfil</span><h2 style={{font:"34px var(--display)",fontWeight:400,margin:"13px 0"}}>Territorio con predominio de {displayDatasetLabel(region.topCrop||"cultivo mixto")}</h2><p className="intro">Este grupo contiene {region.count?.toLocaleString()} observaciones ({Number(region.share||0).toFixed(1)}% de la muestra). Su cultivo más frecuente es <b>{displayDatasetLabel(region.topCrop||"cultivo mixto")}</b>.</p><div style={{marginTop:18}}>{(region.crops||[]).slice(0,6).map(x=><span className="tag" key={x.name||x}>{displayDatasetLabel(x.name||x)}</span>)}</div></div><div className="panel"><h3>Firma ambiental</h3><p className="panel-caption">Valores medios de esta región en unidades nativas.</p><div className="feature-list">{Object.entries(region.profile||{}).map(([k,v])=><div className="feature-line" key={k}><b>{labels[k]||k}</b><div className="spark"><i style={{width:`${Math.min(100,Math.max(8,Number(v)/3))}%`,background:"#719b68"}}/></div><em>{Number(v).toFixed(1)} {units[k]||""}</em></div>)}</div></div></div>}</State></Layout>}
function Recommend(){const [values,setValues]=useState(defaults),[preset,setPreset]=useState("Equilibrado"),[result,setResult]=useState(null),[loading,setLoading]=useState(false),[error,setError]=useState("");const set=(k,v)=>setValues(x=>({...x,[k]:Number(v)}));const run=()=>{setLoading(true);setError("");api("/api/recommend",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(values)}).then(setResult).catch(e=>setError(e.message)).finally(()=>setLoading(false))};return <Layout route="/recommend"><Header eyebrow="04 / Recomendación" title={<>Una segunda lectura<br/><em>para la parcela.</em></>} description="Introduce las condiciones del terreno. Terra las compara con la ecorregión funcional observada más cercana y devuelve un perfil de cultivos basado en evidencia."/><div className="presets" aria-label="Escenarios de ejemplo">{Object.keys(presets).map((k,i)=>{const spanish=["Equilibrado","Trópico húmedo","Secano","Tierra alta"][i];return <button key={k} className={`preset ${preset===spanish?"active":""}`} onClick={()=>{setPreset(spanish);setValues(presets[k])}}>{spanish}</button>})}</div><div className="form-layout"><div className="form-panel"><div className="kicker">Tu terreno · 7 señales</div><h2 style={{font:"28px var(--display)",fontWeight:400,margin:"12px 0 26px"}}>Describe las condiciones</h2><div className="field-grid">{FEATURES.map(k=><div className="field" key={k}><label htmlFor={k}>{labels[k]} <span>{values[k]} {units[k]}</span></label><input id={k} type="range" className="range" min={ranges[k][0]} max={ranges[k][1]} step={k==="pH_Value"||k==="Temperature"?".1":"1"} value={values[k]} onChange={e=>{setPreset("Personalizado");set(k,e.target.value)}}/><input aria-label={`${labels[k]} valor exacto`} type="number" min={ranges[k][0]} max={ranges[k][1]} step={k==="pH_Value"||k==="Temperature"?".1":"1"} value={values[k]} onChange={e=>{setPreset("Personalizado");set(k,e.target.value)}}/></div>)}</div><div className="submit-row"><span className="kicker">Listo para descubrir tu resultado</span><button className="button" onClick={run} disabled={loading}>{loading?"Buscando…":"Ver mi recomendación →"}</button></div>{error&&<div className="error" style={{marginTop:18}}>{error}</div>}</div><div>{result?<div className="result"><span className="eyebrow">Ecorregión funcional coincidente</span><h2>Región {result.cluster}</h2><div className="confidence"><b>{result.confidenceLabel}</b><span>{result.sampleCount?.toLocaleString()} observaciones comparables · {Number(result.share||0).toFixed(1)}% de la muestra</span></div><div className="crop-result"><h3 style={{margin:"0 0 8px",fontSize:13}}>Afinidad de cultivos observada</h3>{(result.crops||[]).map((c,i)=><div className="crop-item" key={c.name||c.crop||i}><b>{displayDatasetLabel(c.name||c.crop)}</b><span>{Number(c.share||c.value||0).toFixed(1)}%</span></div>)}</div>{Array.isArray(result.advice)&&result.advice.length>0&&<div className="note" style={{marginTop:22}}><strong>Lectura de manejo</strong><ul>{result.advice.map((item,i)=><li key={i}>{item}</li>)}</ul></div>}</div>:<div className="panel result-empty"><div><strong>Tu recomendación aparecerá aquí</strong>Ajusta los controles o elige un escenario. Después podrás ver la ecorregión más cercana.</div></div>}</div></div></Layout>}
export default function App(){const path=window.location.pathname.replace(/\/$/,"")||"/";return path==="/analysis"?<Analysis/>:path==="/regions"?<Regions/>:path==="/recommend"?<Recommend/>:path==="/optimize"?<Optimize Layout={Layout}/>:<Overview/>}